# App
APP_ENV=development
JWT_SECRET=replace-with-long-secret
JWT_TTL_HOURS=24

# Seed user
SEED_ADMIN_NAME=System Owner
SEED_ADMIN_EMAIL=admin@gms.local
SEED_ADMIN_PASSWORD=admin123

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=gms
POSTGRES_PASSWORD=gms
POSTGRES_DB=gms
POSTGRES_SSLMODE=disable

# Backend expose port on host
BACKEND_PORT=8080

# Frontend
VITE_API_BASE_URL=http://localhost:8080

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
